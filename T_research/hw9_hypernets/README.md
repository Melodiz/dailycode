# hw9 — shortcut detection with sparse concept probes

Report: [report/main.pdf](report/main.pdf)

## run

```
pip install -r requirements.txt
jupyter notebook hw9_hyper_pcd.ipynb   # run all cells top-to-bottom, ~60 min on CPU, ~8 min on T4
```

Or upload `hw9_hyper_pcd.ipynb` to Google Colab — the first cell installs deps.
